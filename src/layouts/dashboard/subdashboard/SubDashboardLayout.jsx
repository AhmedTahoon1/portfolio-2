import { Outlet } from "react-router";

const SubDashboardLayout = () => {
  return (
    <div className="sub-dashboard-layout">
      <Outlet />
    </div>
  );
};

export default SubDashboardLayout;
